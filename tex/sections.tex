%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Introduction}\label{sec:introduction}
This thesis studies end-to-end LiDAR range-image compression with explicit ROI-aware bit allocation for downstream perception. The core question is whether adaptive latent quantization can preserve task-relevant regions while reducing bitrate. For paper presentation, we report results by model family:
\nocite{*} % Prevent empty bibliography errors before explicit \cite entries are added.
\begin{itemize}
    \item \textbf{Uniform Baseline (UB)}: ROI-unaware uniform latent quantization.
    \item \textbf{Adaptive ROI Student (ARS)}: adaptive quantization with ROI supervision, no teacher.
    \item \textbf{Adaptive Distilled Student (ADS)}: ARS plus teacher distillation.
    \item \textbf{Multi-Scale Distilled Student (MS-ADS)}: ADS objective with stronger multi-scale ROI heads.
\end{itemize}

This document consolidates the actual code path, loss equations, experiment settings, and intermediate-to-latest results from the current repository and logs.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Method and Model Families}\label{sec:method}
\subsection{Input and model pipeline}
Each LiDAR scan is projected to a 5-channel range image $x \in \mathbb{R}^{B \times 5 \times H \times W}$ with channels \texttt{range, intensity, x, y, z}. The forward path is:
\begin{equation}
x \xrightarrow[]{\text{encoder}} z \xrightarrow[]{\text{importance head}} p \xrightarrow[]{\text{quantization}} \hat{z} \xrightarrow[]{\text{decoder}} \hat{x}.
\end{equation}
Backbones are ResNet-style encoder or DarkNet encoder, with latent projection to 64 channels. For MS-ADS variants, multi-stage encoder taps are additionally consumed by the ROI head.

\subsection{Adaptive quantization}
For adaptive mode, the importance map $p \in [0,1]$ controls quantization levels:
\begin{equation}
\ell = b_{\text{bg}} + p\,(b_{\text{roi}} - b_{\text{bg}}),
\end{equation}
followed by rounding/clamping $\tilde{\ell} = \max(2,\text{round}(\ell))$. With per-sample normalization:
\begin{equation}
z_{\text{norm}} = \frac{z - z_{\min}}{z_{\max}-z_{\min}+\epsilon}, \quad
q = \text{round}\!\left(z_{\text{norm}}(\tilde{\ell}-1)\right),
\end{equation}
\begin{equation}
\hat{z} = \frac{q}{\tilde{\ell}-1+\epsilon}(z_{\max}-z_{\min}) + z_{\min}.
\end{equation}
In this codebase, larger importance yields larger level count (finer quantization), not coarser.

\subsection{Model families used in this thesis}
\begin{itemize}
    \item \textbf{UB}: \texttt{quantizer\_mode=uniform}. No teacher, no importance supervision.
    \item \textbf{ARS}: \texttt{quantizer\_mode=adaptive} + ROI supervision from SemanticKITTI ROI mask.
    \item \textbf{ADS}: ARS + teacher distillation (\texttt{pointpillars\_zhulf} backend).
    \item \textbf{MS-ADS}: ADS objective maintained, but ROI head replaced by multi-scale variants (\texttt{bifpn, deformable\_msa, dynamic, rangeformer, frnet}).
\end{itemize}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Loss Formulation}\label{sec:loss}
\subsection{Unified objective}
The implemented training objective is:
\begin{equation}
\mathcal{L}_{\text{total}} =
\lambda_{\text{recon}}\mathcal{L}_{\text{recon}} +
\lambda_{\text{distill}}\mathcal{L}_{\text{distill}} +
\lambda_{\text{rate}}\mathcal{L}_{\text{rate}} +
\lambda_{\text{imp}}\mathcal{L}_{\text{imp}} +
\lambda_{\text{sep}}\mathcal{L}_{\text{sep}}.
\label{eq:total-loss}
\end{equation}

\subsection{Reconstruction loss}
\begin{equation}
\mathcal{L}_{\text{recon}} = \text{MSE}(\hat{x}, x).
\end{equation}

\subsection{Rate proxy loss}
Let normalized level map be:
\begin{equation}
\ell_{\text{norm}} = \frac{\tilde{\ell} - b_{\text{bg}}}{b_{\text{roi}} - b_{\text{bg}}}.
\end{equation}
Three implemented modes are:
\begin{equation}
\mathcal{L}_{\text{rate}}^{\text{global}} = \text{mean}(\tilde{\ell}),
\end{equation}
\begin{equation}
\mathcal{L}_{\text{rate}}^{\text{norm-global}} = \text{mean}(\ell_{\text{norm}}),
\end{equation}
\begin{equation}
\mathcal{L}_{\text{rate}}^{\text{norm-bg}} =
\frac{\sum (\ell_{\text{norm}} \odot w_{\text{bg}})}{\sum w_{\text{bg}}},
\end{equation}
where
\begin{equation}
w_{\text{bg}}=
\begin{cases}
1-m, & \text{if ROI target } m \text{ exists},\\
1-\text{stopgrad}(p), & \text{otherwise}.
\end{cases}
\end{equation}

\subsection{Importance supervision loss}
\begin{equation}
\mathcal{L}_{\text{imp}} =
\text{BCEWithLogits}(s, m; \text{pos\_weight}),
\end{equation}
with logits $s$ from the importance head and ROI target $m$. In \texttt{weighted\_bce} mode:
\begin{equation}
\text{pos\_weight}=
\text{clip}\!\left(\frac{N_{\text{neg}}}{\max(N_{\text{pos}},1)},\,1,\,w_{\max}\right).
\end{equation}
ROI target downsampling mode is one of \texttt{nearest}, \texttt{maxpool}, \texttt{area}.

\subsection{ROI/BG separation loss}
\begin{equation}
\mathcal{L}_{\text{sep}} =
\max\!\left(0,\;\delta - (\mu_{\text{roi}} - \mu_{\text{bg}})\right),
\end{equation}
where $\mu_{\text{roi}}$ and $\mu_{\text{bg}}$ are mean sigmoid importance over ROI and background masks.

\subsection{Distillation loss}
The composite distillation loss is:
\begin{equation}
\mathcal{L}_{\text{distill}} =
\alpha_f \mathcal{L}_f + \alpha_\ell \mathcal{L}_\ell.
\end{equation}
Feature distillation uses weighted MSE between channel-mean student latent and channel-mean teacher feature. Logit distillation uses mode \texttt{auto/kl/bce/mse}; in \texttt{auto}, single-channel logits use BCE and multi-channel logits use KL.

\subsection{Why these losses were selected}
Loss design was changed based on observed failure modes in our experiments, not only by prior expectation.
\begin{itemize}
    \item \textbf{From legacy to normalized rate loss}: legacy used raw \texttt{mean(level\_map)} (\texttt{global\_mean}), which strongly penalized higher importance and contributed to collapse. This behavior is discussed in the project logs as ``importance collapse'' with empty ROI predictions at threshold 0.5.
    \item \textbf{From BCE to weighted BCE}: ROI positives are sparse after heavy downsampling, so weighted BCE with auto \texttt{pos\_weight} was adopted to reduce the trivial all-background solution.
    \item \textbf{Adding separation margin}: \texttt{balanced\_v2} adds ROI/BG separation to improve operating-point selectivity, especially when false positives dominate at fixed threshold.
    \item \textbf{Distillation logit mode}: single-channel logit distillation uses BCE in \texttt{auto} mode to avoid degenerate KL behavior.
\end{itemize}

\begin{table}[!htb]
\caption{Loss-design decisions grounded in experiment evidence}
\label{tab:loss-rationale-evidence}
\centering
\small
\begin{tabular}{l l l}
\toprule
\textbf{Decision} & \textbf{Observed issue} & \textbf{Evidence} \\
\midrule
\texttt{global\_mean} $\rightarrow$ normalized rate &
importance collapse under legacy objective &
threshold-0.5 collapse reports with $TP=0,FP=0,FN>0$ \\
plain BCE $\rightarrow$ weighted BCE &
ROI positive sparsity after downsampling &
negative ROI/BG gap in quick 16-frame diagnostics \\
add separation margin ($\lambda_{\text{sep}}>0$) &
high-FP behavior at fixed threshold &
Cycle-F logs: high recall but poor precision/IoU \\
native vs oracle analysis &
identify bottleneck location &
ARS: $bpp_{ent}$ 0.8064$\rightarrow$0.4177, ADS: 0.8687$\rightarrow$0.4492 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Family-specific objectives and rationale}
\begin{table}[!htb]
\caption{Model-family objectives and motivation}
\label{tab:family-objective}
\centering
\small
\begin{tabular}{l l l}
\toprule
\textbf{Family} & \textbf{Effective default objective} & \textbf{Motivation from experiments} \\
\midrule
UB &
$\mathcal{L}_{\text{total}}=\mathcal{L}_{\text{recon}}$ &
Reference point for ROI-unaware compression. \\
ARS &
$1.0\mathcal{L}_{\text{recon}} + 0.02\mathcal{L}_{\text{rate}} + 1.0\mathcal{L}_{\text{imp}}$ &
Avoid collapse seen in legacy objective. \\
ADS &
$1.0\mathcal{L}_{\text{recon}} + \lambda_d\mathcal{L}_{\text{distill}} + 0.02\mathcal{L}_{\text{rate}} + 1.0\mathcal{L}_{\text{imp}} + 0.2\mathcal{L}_{\text{sep}}$ &
Improve ROI signal quality and calibration. \\
MS-ADS &
Same as ADS, with multi-scale ROI-head ablation &
Test whether architecture capacity improves selectivity. \\
\bottomrule
\end{tabular}
\end{table}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Experimental Setup}\label{sec:setup}
\subsection{Data and training protocol}
The training dataset is SemanticKITTI projected to range images, using sequences \texttt{00,01,02,03,04,05,06,07,09,10}. For fast diagnosis, a 16-frame evaluation subset is also used in notebook-based quick evaluation and oracle/native analysis.

\subsection{Model and optimization defaults}
\begin{itemize}
    \item Input channels: 5.
    \item Latent channels: 64.
    \item Backbones: ResNet encoder or DarkNet encoder.
    \item Adaptive quantization defaults: $b_{\text{roi}}=256$, $b_{\text{bg}}=16$.
    \item Uniform baseline bits: typically 4/6/8 in sweep, and 6/8 in fair-budget pilot.
    \item Optimizer: AdamW; default learning rate $10^{-4}$.
    \item Default ROI target mode in current recipes: \texttt{maxpool}.
\end{itemize}

\subsection{Loss recipe defaults}
\begin{table}[!htb]
\caption{Recipe-level default settings}
\label{tab:recipes}
\centering
\small
\begin{tabular}{l l l}
\toprule
\textbf{Recipe} & \textbf{Rate mode} & \textbf{Importance mode} \\
\midrule
\texttt{legacy} & \texttt{global\_mean} & \texttt{bce} \\
\texttt{balanced\_v1} & \texttt{normalized\_global} & \texttt{weighted\_bce} \\
\texttt{balanced\_v2} & \texttt{normalized\_bg} & \texttt{weighted\_bce} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Teacher and head configurations}
\begin{itemize}
    \item ADS/MS-ADS teacher backend: \texttt{pointpillars\_zhulf}.
    \item Teacher checkpoint: \texttt{data/checkpoints/pointpillars\_epoch\_160.pth}.
    \item High-capacity ROI head: \texttt{pp\_lite} with 1,020,609 parameters.
    \item Teacher adapter network size: 4,834,824 parameters.
    \item Capacity ratio of \texttt{pp\_lite} to teacher: 21.1\%.
\end{itemize}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Results and Intermediate Findings}\label{sec:results}
\subsection{Failure signal that drove loss redesign}
Early experiments exposed a failure mode where importance predictions stayed near floor values, causing empty ROI prediction at threshold 0.5 in some runs. A representative notebook diagnosis reported:
\begin{itemize}
    \item $TP=0$, $FP=0$, $FN=70$ at threshold 0.5.
    \item importance range close to floor (example: min 0.0100, max 0.0307, mean 0.0127).
\end{itemize}
This motivated replacing legacy objective terms with normalized rate loss and weighted ROI supervision.

\subsection{150-epoch full sweeps with revised objectives}
After recipe and head updates, 150-epoch runs were executed. Final losses are listed in Table~\ref{tab:full-150}.

\begin{table}[!htb]
\caption{150-epoch run summary}
\label{tab:full-150}
\centering
\small
\begin{tabular}{l l l c}
\toprule
\textbf{Family} & \textbf{Run ID} & \textbf{Setting} & \textbf{Final loss} \\
\midrule
ARS & \texttt{260216\_resnet\_lr1e-4\_j21424\_r1} & ResNet, lr=$10^{-4}$ & 0.7224 \\
ARS & \texttt{260216\_resnet\_lr5e-5\_j21421\_r3} & ResNet, lr=$5\cdot 10^{-5}$ & 0.7877 \\
ARS & \texttt{260216\_darknet\_lr1e-4\_j21423\_r0} & DarkNet, lr=$10^{-4}$ & 1.6822 \\
ARS & \texttt{260216\_darknet\_lr5e-5\_j21425\_r2} & DarkNet, lr=$5\cdot 10^{-5}$ & 1.7948 \\
\midrule
ADS & \texttt{260216\_darknet\_ld0.1\_j21422\_r2} & $\lambda_d=0.1$ & 2.1185 \\
ADS & \texttt{260216\_darknet\_ld0.5\_j21426\_r0} & $\lambda_d=0.5$ & 2.7526 \\
ADS & \texttt{260216\_darknet\_ld1.0\_j21427\_r1} & $\lambda_d=1.0$ & 3.1818 \\
\bottomrule
\end{tabular}
\end{table}

Main trend: in this setting, ADS distillation sweep favored $\lambda_d=0.1$ over larger values, and ARS with ResNet produced lower objective values than ARS with DarkNet.

\subsection{Quick 16-frame analysis snapshot}
Quick evaluation over 16 frames showed that fixed-threshold ROI selectivity remained unstable. Example summary values:
\begin{itemize}
    \item UB (ResNet q8): all-MSE 13.4235, ROI-MSE 24.8127.
    \item ARS (ResNet): all-MSE 10.9570, ROI-MSE 19.8815.
    \item ADS (DarkNet, $\lambda_d=0.1$): all-MSE 29.5963, ROI-MSE 86.1911.
\end{itemize}
These values are directly from \texttt{notebooks/quick\_eval\_summary\_16frames.csv}. Across several runs, large false-positive behavior and threshold sensitivity were observed.

\subsection{Fair-budget pilot and oracle decomposition}
To separate ``head quality'' from ``quantizer potential,'' a controlled pilot compared native predicted importance against oracle ROI importance (same checkpoints). Key 16-frame results:
\begin{table}[!htb]
\caption{Native vs oracle ROI pilot (16-frame subset)}
\label{tab:oracle-pilot}
\centering
\small
\begin{tabular}{l l c c}
\toprule
\textbf{Run} & \textbf{Mode} & \textbf{$bpp_{\text{entropy}}$} & \textbf{ROI-MSE} \\
\midrule
UB ResNet q6 & native & 0.6668 & 228.0958 \\
UB ResNet q8 & native & 0.9598 & 227.3080 \\
ARS (adaptive) & native & 0.8064 & 227.8031 \\
ARS (adaptive) & oracle ROI & 0.4177 & 227.8031 \\
ADS (adaptive+distill) & native & 0.8687 & 227.6069 \\
ADS (adaptive+distill) & oracle ROI & 0.4492 & 227.6073 \\
\bottomrule
\end{tabular}
\end{table}

Interpretation: with ideal ROI signal (oracle mode), bitrate proxy drops strongly at almost unchanged ROI-MSE. This indicates the current bottleneck is primarily importance-map quality/calibration, not only quantizer formulation.

\subsection{MS-ADS ablation status}
MS-ADS multi-scale head ablations are already implemented and running on both backbones:
\begin{itemize}
    \item ResNet array job: \texttt{21834}.
    \item DarkNet array job: \texttt{21840}.
    \item Head variants: \texttt{bifpn}, \texttt{deformable\_msa}, \texttt{dynamic}, \texttt{rangeformer}, \texttt{frnet}.
\end{itemize}
At the time of writing, these runs are still in progress, so final MS-ADS quantitative comparisons are deferred.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Discussion}\label{sec:discussion}
\subsection{What improved}
The transition from legacy objective to balanced recipes, combined with stronger ROI head capacity and real teacher wiring, removed several early reproducibility and training-instability issues. Logging and ledger automation also enabled consistent cross-run analysis.

\subsection{What remains unresolved}
Two issues remain central:
\begin{itemize}
    \item importance calibration instability across runs (threshold-sensitive behavior),
    \item weak ROI selectivity at fixed threshold, often with high false positives.
\end{itemize}
Therefore, low final training loss alone cannot support deployment claims; detector-facing operating-point analysis is required.

\subsection{Planned visual analysis (next iteration)}
This draft intentionally omits figures. The next writing iteration should add:
\begin{itemize}
    \item threshold sweep curves (precision/recall/IoU vs threshold) per stage,
    \item native vs oracle bitrate-distortion plots at matched budget,
    \item qualitative triples: input range/intensity, GT ROI, predicted importance/ROI.
\end{itemize}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Conclusion}\label{sec:conclusion}
This thesis draft documents an implemented LiDAR compression pipeline organized as UB, ARS, ADS, and MS-ADS model families, with explicit code-faithful objectives and experiment settings. Intermediate and latest results show that adaptive quantization has clear potential under reliable ROI signals, but current performance is constrained by importance-map calibration and selectivity. Ongoing MS-ADS multi-scale head experiments are designed to directly address this bottleneck and complete detector-facing validation.
